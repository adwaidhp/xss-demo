services:
  - type: web
    name: xss-demo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --preload --worker-class sync
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
